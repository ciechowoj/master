#include <vector>
#include <glm/glm.hpp>

using namespace glm;

struct camera_t {
	vec3 position;
	vec3 direction;
	float aspect;
	float fovy;
};

struct scene_t {
		
};

void raytrace(int width, int height, const camera_t& camera, const scene_t& scene) {


}









